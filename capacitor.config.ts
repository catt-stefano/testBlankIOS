import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mytest.isblank',
  appName: 'testBlankIOS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
